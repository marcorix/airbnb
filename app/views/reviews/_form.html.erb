<%= simple_form_for([flat, review], data: {
  insert_review_target: "form",
  action: "submit->insert-review#send"
  }) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 1..5  %>
  <%= f.button :submit, class: 'btn btn-ghost' %>
<% end %>
